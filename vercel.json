{
  "name": "qa-sentinel-platform",
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "installCommand": "true",
  "buildCommand": "npm install -g pnpm@9.12.3 && pnpm install && pnpm build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ."
}
